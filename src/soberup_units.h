#pragma once

typedef enum Gender {
  Gender_Male,
  Gender_Female,
} Gender;

typedef float Weight;

typedef enum WeightUnits {
  WeightUnits_Pounds,
  WeightUnits_Kilograms,
} WeightUnits;

typedef size_t Grams;
